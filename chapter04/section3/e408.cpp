#include <iostream>

#include <string>
#include <cstring>
using std::string;

#include <vector>
using std::vector;

using std::cin;
using std::cout;
using std::endl;

using std::begin;
using std::end;

int main()
{
  // && RHS evaluated only if LHS is true
  // || RHS evaluated only if LHS is false
  // dont use  expr == true as expr may not have the type bool
  return 0;
}


