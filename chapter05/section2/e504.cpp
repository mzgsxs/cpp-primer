#include <iostream>

#include <string>
#include <cstring>
using std::string;

#include <vector>
using std::vector;

using std::cin;
using std::cout;
using std::endl;

using std::begin;
using std::end;

int main()
{
  //while (string::iterator iter != s.end()) { /* . . . */ }
  // new iter every time!
  //string::iterator iter = ...;
  //while (iter != s.end()) { ... }

  //while (bool status = find(word)) { /* . . . */ }
  //if(!status){/* ... */}

  while (bool status = false) { cout << "ok"; }
  // recall (=) returns LHS value
  int j;
  int i = j = 1;

  cout << endl;
  return 0;
}


