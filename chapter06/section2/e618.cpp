#include <iostream>

#include <string>
#include <cstring>
using std::string;

#include <vector>
using std::vector;

using std::cin;
using std::cout;
using std::endl;

using std::begin;
using std::end;

#include<stdexcept>
using std::exception;
using std::runtime_error;

class Matrix;

bool compare(Matrix const &, Matrix const &);
vector<int>::iterator change_val(int const, vector<int>::iterator );

int main() 
{

  return 0;
}

